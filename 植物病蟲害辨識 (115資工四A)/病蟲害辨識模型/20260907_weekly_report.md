# 柑橘病蟲害辨識階段性進度週報（2026-09-07）

> **報告日期**：2026-09-07（報告期間：2026-09-06 至 2026-09-07，銜接 [2026-08-25 週報](20260825_weekly_report.md)）
> **評測對象**：YOLO26-small-P2（v12s 模型容量探索）對照 YOLO26-nano-P2（v11.5 現行交付基準）
> **資料來源**：訓練用程式碼庫的 `docs/` 記錄文件與 `Train Code/v12s/` 評估輸出（詳細實驗日誌見程式碼庫，不在本文件庫版控範圍）；表 5-1 前六列取自 2026-06/07 的歸檔訓練輸出（`results.csv`、`training_metrics.csv`），同樣不在本文件庫版控範圍
> **撰寫人**：Claude Code

## 1. 摘要

- **本期用預先登記的判準同時關掉了兩個方向。** 一是模型容量（v12s），二是部署端的匯出參數調校；兩者都不是憑印象判斷，而是達標線在開跑前就寫死。
- **v12s 判準未通過：模型參數放大 3.84 倍，test mAP50 只從 0.80959 變成 0.81266，增加 0.00307**，僅為 0.03 門檻的十分之一，且遠低於本專題量過的 run 間全距 0.021。**結論是模型容量不是瓶頸。**
- **九個類別中只有一類的變化超過 ±0.04 的 per-class 雜訊地板**，四類變好、四類變差、一類不動。若容量真的是瓶頸不會呈現這種分佈，尤其第二弱的 `Citrus_Leaf_Miner` 反而掉了 0.024。
- **這是模型端連續第四輪無效**，累計十一種改動。唯一有效過的手段仍然是資料端的標註重整。因此 **m 尺度不需要再試**：s 尺度的 3.84 倍參數只換到雜訊等級的差異，m 尺度沒有理由不同，成本卻會再上去。
- **部署端已量清楚：測試裝置達不到 30 FPS**，精度可用的最佳組合為 15.42 FPS。完整數據見〈[YOLO26-nano-P2 匯出參數與行動端延遲全面掃描報告](../%E7%B3%BB%E7%B5%B1%E6%B8%AC%E8%A9%A6%E8%88%87%E8%A9%95%E4%BC%B0/yolo26n_p2_export_params_benchmark.md)〉。

### 核心評估指標總覽

| 版本與權重 | 參數量 | test mAP50 | test mAP50-95 | Precision | Recall |
| --- | ---: | ---: | ---: | ---: | ---: |
| v11.5（YOLO26-nano-P2，現行交付基準） | 2,518,088 | 0.80959 | 0.60691 | — | — |
| **v12s（YOLO26-small-P2，`last.pt`）** | **9,667,624** | **0.81266** | **0.61836** | 0.8664 | 0.8044 |
| 差異 | 3.84 倍 | **+0.00307** | +0.01145 | — | — |
| **預先登記的達標線** | — | **0.83959** | — | — | — |

## 2. 背景與方法

本期要回答的是一個明確的問題：**模型容量是不是本專題精度的瓶頸？** 在此之前，模型端的架構與損失函數改動已連續三輪未通過顯著性門檻，但「換一個更大的模型」始終沒有被實際測過。

### 2.1 單一變因設計

v12s 相對 v11.5 **只改一個變因：模型尺度由 nano 換成 small**。預訓練權重、排程、全部超參數、資料集版本與切分方式逐字相同。

| 項目 | v11.5 | v12s |
| --- | ---: | ---: |
| 參數量 | 2,518,088 | 9,667,624（3.84 倍） |
| GFLOPs（640） | 7.65 | 26.67（3.49 倍） |
| epochs 與 patience | 70 與 0 | 70 與 0 |
| 其餘超參數 | — | 完全相同 |

訓練中 `batch=20` 沒有發生記憶體不足，未曾降低批次大小，**所以本輪確實是純粹的尺度比較，沒有混入其他變因**。

### 2.2 判準為何訂在 0.03，以及為何事先訂

達標線 test mAP50 大於等於 0.83959（v11.5 的 0.80959 加 0.03）是**開跑前就寫死在訓練腳本第一格**的，不是事後決定。理由有兩項實測依據：

- **per-class 雜訊地板約 ±0.04**：在 v11 上，資料完全沒有改動的類別也會移動這個幅度。
- **run 間全距 0.021**：v10、v11、v11.5 三個版本在完全同一批影像上的 test mAP50 為 0.809、0.800、0.788。

低於 0.03 的差距無法與這兩種雜訊區分，所以事先約定那不算數。**先訂判準再跑實驗，結果才有拒絕的能力。**

### 2.3 訓練協定

一律採 `patience=0`、固定輪數、報告 `last.pt`。這是 v11.5 起確立的協定：`best.pt` 由 valid 挑出，一旦採用 `best.pt`，valid 就參與了決策而不能併入評估集；改報 `last.pt` 之後 valid 可合法併計，評估誤差從只報 test 的 ±0.140 降到 ±0.099。

## 3. v12s：模型容量是不是瓶頸

### 3.1 判準裁決

#### 表 3-1：整體指標對照

| 指標 | v11.5 | v12s | 差異 | 判讀 |
| --- | ---: | ---: | ---: | --- |
| test mAP50 | 0.80959 | 0.81266 | **+0.0031** | 雜訊內 |
| test mAP50-95 | 0.60691 | 0.61836 | +0.0115 | 雜訊內 |
| valid mAP50 | 0.82899 | 0.83652 | +0.0075 | 雜訊內 |
| valid mAP50-95 | 0.63580 | 0.64345 | +0.0077 | 雜訊內 |

表 3-1 顯示四個指標全部落在雜訊範圍內，主要結果 test mAP50 的增幅只有達標門檻的十分之一，**判準未通過**。

### 3.2 逐類別結果

#### 表 3-2：九個類別的 AP50 變化

| 類別 | v11.5 | v12s | 差異 | 判讀 |
| --- | ---: | ---: | ---: | --- |
| **Scale_Insect** | 0.6704 | 0.7112 | **+0.0408** | **可辨識的改善** |
| Thrips_Damage | 0.4515 | 0.4885 | +0.0371 | 雜訊內 |
| Canker | 0.8677 | 0.9016 | +0.0340 | 雜訊內 |
| Thrips | 0.7795 | 0.7526 | −0.0268 | 雜訊內 |
| Oily_Spot | 0.9446 | 0.9204 | −0.0242 | 雜訊內 |
| Citrus_Leaf_Miner | 0.6764 | 0.6523 | −0.0241 | 雜訊內 |
| Black_Spot | 0.9534 | 0.9486 | −0.0048 | 雜訊內 |
| Aphid | 0.9478 | 0.9436 | −0.0042 | 雜訊內 |
| Sooty_Mold | 0.9950 | 0.9950 | 0.0000 | 無變化 |

表 3-2 是本輪最有說服力的證據：四類變好、四類變差、一類不動，只有 `Scale_Insect` 勉強越過 ±0.04 的雜訊地板。**若容量真的是瓶頸，應該看到多數類別同向改善，尤其是最弱的那幾類；但第二弱的 `Citrus_Leaf_Miner` 反而變差 0.024。** 這個分佈就是「沒有系統性改善」的樣子。

### 3.3 成本

#### 表 3-3：放大模型的代價

| 項目 | v11.5 | v12s | 倍率 |
| --- | ---: | ---: | ---: |
| 訓練時間 | 3.06 小時 | 4.91 小時 | 1.61 倍 |
| 每輪耗時 | 157 秒 | 253 秒 | 1.61 倍 |
| 最佳輪次 | 第 53 輪 | 第 69 輪 | — |
| 行動端推論延遲（依實測外推） | 266 ms | 約 950 ms | 3.5 倍 |

表 3-3 顯示訓練成本的增幅（1.61 倍）遠低於 GFLOPs 的增幅（3.49 倍），代表 nano 尺度時 GPU 並未吃滿，資料管線是相當大的固定成本；但**推論端沒有這個緩衝**，因此 v12s 在行動端約 1.05 FPS。

> **v12s 不是候選部署模型。** 行動端連 nano 尺度的 266 ms 都離 30 FPS 差 6.8 倍，small 尺度只會更遠。它的定位是精度上限探索，用來回答容量是不是瓶頸，答案是不是。

### 3.4 附帶確認的三件事

- **`patience=0` 與 `last.pt` 的協定再次成立**：`best.pt` 與 `last.pt` 在 test 與 valid 上的差距全部小於等於 0.0007，且方向不一致。`best.pt` 高出平台期僅 0.77 個標準差，是歷來最小（v10 為 2.8、v11 為 2.7、v11.5 為 1.5）。
- **評估精度維持達標**：九個類別的 pooled ±2SE 最差為 0.099（`Citrus_Leaf_Miner`），全數小於等於 0.10。
- **本輪對兩項事前預測的修正**：原先預測參數放大後會提早過擬合、最佳輪次應早於第 53 輪，實際是第 69 輪且完全沒有過擬合跡象；訓練時間原估 7.6 至 9.1 小時，實際 4.91 小時，誤在以 GFLOPs 線性外推訓練時間。**往後估訓練時間不可用 GFLOPs 外推**，推論端可以，訓練端會高估。

## 4. 模型端的累積結論

### 表 4-1：模型端四輪改動的結果

| 輪次 | 改動內容 | 結果 |
| --- | --- | --- |
| v9 六臂消融 | Wise-Inner-MPDIoU（兩種比例）、ADown、StarTripletBlock、輸入解析度 800 | **零臂通過 2 個標準差門檻** |
| v10 至 v11 至 v11.5 | 三次訓練協定與資料切分調整 | 同一批影像上全距 0.021，等於 run 間變異 |
| **v12s** | **模型尺度放大 3.84 倍參數** | **+0.003，等於雜訊** |

表 4-1 顯示模型端已連續四輪、累計十一種改動全部無效。**唯一真正有效過的手段是資料端的標註重整**：v5 至 v5r 的標註尺度統一讓 Thrips 的 AP50 從 0.572 提升到 0.749，用的是原封不動的官方架構。

由此得出兩條明確的停損：**不要再試 m 尺度模型**（s 尺度的 3.84 倍參數只換到雜訊等級的差異，m 尺度的 21.07 M 參數沒有理由不同，訓練與推論成本卻會再上去）；**不要再從模型端找改善**。

## 5. 歷史訓練成果總覽（2026-06 至 v12s）

> [!IMPORTANT]
> **本節各表的每一列都不可直接比較。** 各期用的是不同的資料集，
> 類別數與評估集組成都不同，數值高低不代表模型能力的高低。詳見 5.6。

### 表 5-1：全期總覽（2026-06 至 2026-09，valid split）

本表涵蓋**全期（2026-06 → 2026-09）**。統一取 **valid split** —— 那是唯一每一期都有的量測；
`test` 只有 v9 之後才有，見下一節。

| # | 期別 | 模型 / Run | 資料集 | 類別 | 報告權重 | mAP50 | mAP50-95 | Precision | Recall | Jaccard |
| ---: | --- | --- | --- | ---: | --- | ---: | ---: | ---: | ---: | ---: |
| 1 | 2026-06/07 | YOLO26-large | 12 類 | 12 | 最後一輪（ep109） | **0.87106** | **0.74360** | 0.89832 | 0.84359 | 0.7701 † |
| 2 | 2026-06/07 | YOLO26-nano | 12 類 | 12 | 最後一輪（ep124） | 0.85443 | 0.72053 | 0.86795 | 0.82437 | 0.7325 † |
| 3 | 2026-06/07 | YOLO26-nano+P2 | 12 類 | 12 | 最後一輪（ep98） | 0.85113 | 0.72013 | 0.87695 | 0.83082 | 0.7441 † |
| 4 | 2026-06/07 | YOLO26n-P2 微調 | 12 類 | 12 | 最後一輪（ep7） | 0.84629 | 0.71317 | 0.86624 | 0.81009 | 0.7200 † |
| 5 | 2026-06/07 | SSD-MobileNetV3-large | 12 類 | 12 | ep36 | 0.4717 | 0.3269 | 0.1870 ‡ | 0.1471 ‡ | 0.0897 †‡ |
| 6 | 2026-06/07 | SSD-MobileNetV3-small | 12 類 | 12 | ep40 | 0.4527 | 0.2676 | 0.3422 ‡ | 0.0850 ‡ | 0.0731 †‡ |
| 7 | 2026-08 | v8 | v5 | 8 | `best.pt`（ep69） | 0.82082 | 0.64688 | 未存檔 | 未存檔 | 未存檔 |
| 8 | 2026-08 | v9（A0 交付版） | v5r | 8 | `best.pt`（ep69） | 0.86651 | 0.65356 | 0.88595 | 0.80756 | 0.65353 |
| 9 | 2026-09 | v10 | v5.5 | 9 | `best.pt`（ep54） | 0.79931 | 0.59268 | 0.85617 | 0.77412 | 0.64277 |
| 10 | 2026-09 | v11 | v5.6 | 9 | `best.pt`（ep69） | 0.83543 | 0.62924 | 未存檔 | 未存檔 | 0.66056 |
| 11 | 2026-09 | **v11.5（現行交付基準）** | v5.6 | 9 | `last.pt` | **0.82899** | **0.63580** | **0.86341** | **0.80114** | **0.65425** |
| 12 | 2026-09 | **v12s** | v5.6 | 9 | `last.pt` | **0.83652** | **0.64345** | **0.86186** | **0.82734** | 未存檔 |

> **† 這六列的 Jaccard 與其他列不是同一種算法。** 2026-06/07 的報告用
> `1/(1/P + 1/R − 1)` 由 `val()` 的 P、R **代數換算**（工作點 conf≈0.001）；
> v9 之後改為**由混淆矩陣實際計數** TP/FP/FN（工作點 conf=0.25）。
> **兩者不可放在一起比較**，此處並列僅為完整記錄。
>
> **‡ SSD 的 P/R 是 conf=0.20 的單點值**，與 mAP 不同工作點，也與 YOLO 的 `val()` P/R 不同。

#### 讀表 5-1 前必須知道的三件事

1. **前六列用的是完全不同的資料集。** 那是 12 類標註（`H_MC`、`H_PK` 兩個健康葉類別，
   加上 `P_AP_LD`、`P_TP_LD` 兩個**零標註**的類別），train 9,276 / valid 438 / test 440。
   現行的 9 類正好等於它**扣掉兩個健康葉類別與零標註的 `P_AP_LD`**。
   評估集只有 438 張且類別分佈極偏（`P_SI` 一類佔 valid 框數 68.8%）。
2. **前六列的「報告權重」是最後一輪，不是最佳輪次。** 原報告標成「最優」，
   但實際峰值都更早（large ep55 = 0.88232、nano ep55 = 0.86713、nano+P2 ep51 = 0.86568、
   微調 ep2 = 0.85520），**四個模型都被低估 0.009–0.015 mAP50**。
   此處沿用原報告的最後一輪值以保持可追溯，引用時請說「最後一輪」。
3. **數值由高到低不是時間順序上的退步。** 第 1 列的 0.871 與第 11 列的 0.829
   之間隔著三次資料集重建、一次洩漏清除與兩次評估集重切 —— 量的根本不是同一件事。


表 5-1 橫跨三套資料集與兩種 Jaccard 算法，**只能當作「某一期當時量到什麼」的紀錄**。
下面兩張表則限定在 v9 之後、同一條資料集演進線上，欄位定義一致。

### 表 5-2：v9 之後 test split 的完整指標（各版本的主要結果）

| 版本 | 資料集 | 類別數 | 報告權重 | mAP50 | mAP50-95 | Precision | Recall | Detection Jaccard |
| --- | --- | ---: | --- | ---: | ---: | ---: | ---: | ---: |
| v9（A0 交付版） | v5r | 8 | `best.pt`（ep69） | 0.89079 | 0.67829 | 0.89950 | 0.86139 | 0.66979 |
| v10 | v5.5 | 9 | `best.pt`（ep54） | 0.80928 | 0.60605 | 0.85456 | 0.77909 | 0.71906 |
| v11 | v5.6 | 9 | `best.pt`（ep69） | 0.82213 | 0.61294 | 未存檔 | 未存檔 | 未存檔 |
| **v11.5（現行交付基準）** | v5.6 | 9 | `last.pt` | **0.80959** | **0.60691** | **0.85839** | **0.79502** | **0.68627** |
| **v12s** | v5.6 | 9 | `last.pt` | **0.81266** | **0.61836** | **0.86643** | **0.80441** | **0.72737** |

表 5-2 只有最後兩列（v11.5 與 v12s）位於同一套資料集且同一種權重選取協定，**是唯一可以直接相減的一組**；v11 雖然同為 v5.6，但它用 `patience=30` 與 `best.pt`，協定不同，且其混淆矩陣只存了 valid 一份，test 的三個逐框指標無法回算。

### 表 5-3：v9 之後 valid split 的完整指標

| 版本 | mAP50 | mAP50-95 | Precision | Recall | Detection Jaccard | TP / FP / FN |
| --- | ---: | ---: | ---: | ---: | ---: | --- |
| v9（A0 交付版） | 0.86651 | 0.65356 | 0.88595 | 0.80756 | 0.65353 | 1841 / 704 / 272 |
| v10 | 0.79931 | 0.59268 | 0.85617 | 0.77412 | 0.64277 | 556 / 136 / 173 |
| v11 | 0.83543 | 0.62924 | 未存檔 | 未存檔 | 0.66056 | 685 / 169 / 183 |
| v11.5 | 0.82899 | 0.63580 | 0.86341 | 0.80114 | 0.65425 | 685 / 179 / 183 |
| v12s | 0.83652 | 0.64345 | 0.86186 | 0.82734 | 未存檔 | 未存檔 |

表 5-3 的 v9 一列 FP 高達 704，是因為 `Scale_Insect` 單一類別就貢獻了 563 個背景誤報；這也是為什麼 v9 的 mAP50 明明最高，Detection Jaccard 卻是全表偏低的一組 —— 兩個指標衡量的是不同的東西。

### 5.4 兩組指標來自不同的工作點

這是讀這些表最容易出錯的地方，必須寫清楚：

| 欄位 | 來源 | 信心閾值 |
| --- | --- | --- |
| mAP50、mAP50-95、Precision、Recall | ultralytics `val()` | **conf = 0.001** |
| Detection Jaccard、TP / FP / FN | 混淆矩陣 | **conf = 0.25** |

**同一列的 Precision 與 Jaccard 不是在同一個工作點上算出來的**，兩者不能互推。
以 v9 為例，同一顆權重在 conf = 0.4985 下的 Jaccard 是 0.67733（valid），
比 conf = 0.25 的 0.65353 高，但 mAP50 會從 0.86651 掉到 0.77299 ——
高閾值必然壓低 mAP。**引用時務必連同閾值一起講。**

Detection Jaccard 的定義是 $\sum \text{TP} / \sum(\text{TP} + \text{FP} + \text{FN})$，
略過物件偵測情境下人為約定的 TN，是比 Accuracy 更可信的整體正確率替代指標。
完整定義見〈[效能指標評估](../README.md#效能指標評估)〉。

### 5.5 只有兩組數字可以直接相減

| 可比較的一組 | 為什麼 |
| --- | --- |
| **v11.5 對 v12s** | 同資料集 v5.6、同排程、同權重協定，只差模型尺度。這就是 §3 的主結果 |
| v10 對 v11 對 v11.5 | 同為 9 類且切分巢狀相容，可在 v5.5 的 326 張子集上重算對照（全距 0.021） |

其餘任兩列相減都沒有意義，跨資料集的更是如此。

### 5.6 為什麼舊版的數字最高卻不代表最好

v9 的 test mAP50 是全表最高的 0.89079，但它**不能拿來說「後來變差了」**，有三個原因：

1. **v5r 有 13.4% 的跨 split 近重複影像**（三個 Pests 類別），評估集裡有模型在訓練時看過的近乎相同的照片，**數字被灌水**。v5.5 起已全類別清零。
2. **v9 只有 8 個類別，沒有 `Thrips_Damage`。** 該類別是九類中最弱的一項（v12s 為 0.488），把它加進平均自然會把整體拉低。v10 在 v5.5 上取 8 類等效值是 0.864 / 0.658，與 v9 的 0.891 / 0.678 只差 0.02 至 0.027，而 v9 那側還受洩漏灌水 —— **實質接近持平，不是退步**。
3. **評估集本身換過。** v5.6 的 valid 與 test 各為 401 張，是為了把每個類別的評估誤差壓到 ±2SE 小於等於 0.10 而重新切分的；量尺變了，刻度就不能互換。

2026-06/07 那六列（表 5-1 第 1 至 6 列）還要再加兩個理由：

4. **那是 12 類資料集，其中兩類零標註**（`P_AP_LD`、`P_TP_LD`），另有兩個健康葉類別（`H_MC`、`H_PK`）。健康葉相對容易辨識，納入平均會把整體拉高；而該版 valid 僅 438 張，且 `P_SI` 一類就佔框數 68.8%。
5. **那些數字是最後一輪，卻在原報告中被標成「最優」。** 依原始 `results.csv` 重算，實際峰值都更早（YOLO26-large 在 ep55 為 0.882），四個模型都被低估 0.009 至 0.015 mAP50。

> **一句話**：這張表適合回答「某一版當時量到什麼」，不適合回答「哪一版比較強」。
> 跨資料集的強弱比較在本專題**沒有合法的基準**，唯一的例外是同資料集內的對照。

## 6. 全案盤點與後續方向

### 6.1 三條線的現況

| 線 | 現況 | 成熟度 |
| --- | --- | --- |
| **資料集** | v5.6：9 類，train 7,264、valid 401、test 401；九類評估誤差 ±2SE 全部小於等於 0.10；全類別零跨 split 近重複；增強造成的標註框膨脹缺陷已修 | 量尺已達標，但兩個類別的**標註品質**仍是開放問題 |
| **模型** | 交付基準為 v11.5（test mAP50 0.80959）；訓練協定已固定 | **已走到底**，四輪十一種改動全部無效 |
| **部署** | 本期新開；22 組量完整 mAP、18 組量實機延遲；最佳可用組合 15.42 FPS | **已量清楚**，官方參數已窮盡 |

本期最大的結構性變化是第三條線從無到有。在此之前專案只有精度這一把尺、量不出延遲，「要不要換更大的模型」根本無法討論，因為不知道有多少預算可用。

### 6.2 還開著的問題

| 問題 | 卡在哪 | 需要什麼 |
| --- | --- | --- |
| **`Thrips_Damage` AP50 僅 0.489** | 成因是**標註框定義不一致**，valid 與 test 差 0.202，三輪都沒有隨評估集擴大而收斂。補樣本量救不了。**且它是九類中標註歷史最短的一類**：2026-06 的 12 類資料集裡已有 `P_TP_LD` 這個代號，但 train / valid / test 全部零標註，v5.5 才第一次真正建立，現也僅 273 張 / 313 框 | 人工標註一致性測試：兩人各標 20 張，中位 IoU 大於等於 0.85 才值得全類重標，否則依既定規則刪除該類別 |
| **`Citrus_Leaf_Miner` AP50 僅 0.652** | 樣本量少，原始影像僅 111 張 | 外部影像 150 張補標框 |
| **達不到 30 FPS** | 官方匯出參數已窮盡，僅剩量化感知訓練與直接在 320 解析度訓練兩條路，都須重訓 | **需先確認產品端是否真的需要即時串流** |
| 結論只成立於一台裝置 | 目前只有 CPH2641 | 第二台測試裝置 |

**上表前兩項是目前唯一還沒試過的方向。** 容量、架構、訓練協定與匯出參數都已排除之後，剩下的手段都在資料端，而資料端卡在人工，不卡在技術。

### 6.3 下一步的兩個決策點

1. **30 FPS 是不是真的需要。** 若互動形式是對準葉片後拍照出結果，64.8 ms 的單張延遲使用者不會察覺，320 解析度的 fp32 現在就可以交付；若必須是即時串流，本裝置做不到，需換硬體或重訓。這是產品決策，技術面已經量清楚。
2. **人工標註工作是否發派。** 兩包工作的規格與判準都已備妥且尚未開始，這是精度線上唯一還有機會的方向。 另有一項新依據：`Thrips_Damage` 是九類中唯一從零開始、且最晚建立標註的類別，因此工作包 A 要驗證的不只是「標得一不一致」，而是**這套標註約定本身是否站得住**。

## 7. 結論與限制

1. **模型容量不是本專題的精度瓶頸。** 3.84 倍參數換到的 0.00307 與同設定重跑一次的隨機變異無法區分，判準未通過。
2. **模型端已無可投入的方向。** 四輪十一種改動全部無效，預算應移回資料端。
3. **現行交付基準維持 v11.5 的 YOLO26-nano-P2。** v12s 不進入部署候選。

本報告未涵蓋：

- **行動端實機推論延遲的完整數據**：依章節職責界線，實機量測數據放在〈[系統測試與評估](../README.md#系統測試與評估)〉，本報告只引用結論。
- **資料集本身的統計**：類別分佈與切分細節見〈[資料集分析](../README.md#資料集分析)〉。
- **m 尺度模型的實測**：本輪據 s 尺度的結果判定不值得投入，屬於推論而非量測，若日後有相反證據應重新評估。
- **run 間變異的重複量測**：本專題的 0.021 全距來自三個不同版本在同一批影像上的結果，不是同一設定重複執行多次的嚴格量測。
- **標註品質問題的解法驗證**：兩包人工標註工作尚未開始，其效益目前無法評估。
