# 效能指標定義與評測方法

> **報告類型**：指標定義（設計／說明類）
> **涵蓋範圍**：影像辨識模組、RAG 與微調 SLM 模組
> **資料來源**：Notion 匯出原始記錄，2026-08-27 依 README §6.2 由 L1 章索引拆分為本篇 L2 報告
> **撰寫人**：原始記錄未標註

## 1. 摘要

- 本報告只定義「怎麼算」，不含任何實測數值；實測結果請見〈[系統測試與評估](../README.md#系統測試與評估)〉
- 影像辨識模組定義 Precision/Recall/F1/Accuracy/IoU/mAP@50/mAP@50-95 七項核心指標
- RAG／SLM 模組定義 13 項裝置執行效率指標與 6 項 RAGAS 品質指標
- RAGAS 技術與評測手冊完整收錄評測框架的指標定義與必要輸入欄位

## 2. 影像辨識模組效能指標

物件偵測任務中，因無效背景框數（TN）為無限且不可統計，全書一律簡化為 $\text{TN} = 0$。

- **Precision（精確率）**：模型預測出的所有目標中，預測正確的比例。
    $$\text{Precision} = \frac{\text{TP}}{\text{TP} + \text{FP}}$$
- **Recall（召回率）**：驗證集中所有真實存在的目標中，被模型成功檢出的比例。
    $$\text{Recall} = \frac{\text{TP}}{\text{TP} + \text{FN}}$$
- **F1-Score**：精確率與召回率的調和平均數。
    $$\text{F1-Score} = 2 \times \frac{\text{Precision} \times \text{Recall}}{\text{Precision} + \text{Recall}}$$
- **Accuracy（準確率，TN=0 簡化版）**：邊界框級別的匹配度，可化簡為只用 Precision 與 Recall 表示。
    $$\text{Accuracy} = \frac{\text{TP}}{\text{TP} + \text{FP} + \text{FN}} = \frac{1}{\frac{1}{\text{Precision}} + \frac{1}{\text{Recall}} - 1}$$
- **IoU（交併比，Intersection over Union）**：衡量預測框 $A$ 與真實框 $B$ 的重疊程度。
    $$\text{IoU} = \frac{\text{Area}(A \cap B)}{\text{Area}(A \cup B)}$$
    當 $\text{IoU} \ge 0.5$ 且預測類別正確時，判定為候選正確預測。
- **mAP@50**：IoU 閾值固定為 0.5 時，所有類別 AP 的平均值，評估粗定位與分類性能。
- **mAP@50-95**：IoU 閾值從 0.5 漸進到 0.95（步長 0.05）分別計算 mAP，再取平均值，評估精密定位性能。

**匹配邏輯**：預測框依信心分數（Confidence Score）由高到低排序，只保留高於特定閾值者；對每個預測框尋找同類別且 IoU 最大的真實框，若 $\text{IoU} \ge 0.5$ 且該真實框未被匹配過則記為 **TP**，否則記為 **FP**；遍歷完畢後所有未被匹配的真實框記為 **FN**。

（本節整理自〈[全模型訓練數據報告](../%E7%97%85%E8%9F%B2%E5%AE%B3%E8%BE%A8%E8%AD%98%E6%A8%A1%E5%9E%8B/20260714_all_models_training_metrics.md)〉§3，該報告的完整推導過程可參考原文）

## 3. RAG 與微調 SLM 模組效能指標

### 3.1 裝置執行效率

| **編號** | **指標名稱 (Metric Name)** | **分類 (Category)** | **定義與說明 (Definition & Description)** | **單位/格式** |
| --- | --- | --- | --- | --- |
| **1** | **Preprocess Time**
(語境/文字預處理時間) | RAG 流程延遲 | 包含執行關鍵字匹配與句子擷取 的時間。 | 毫秒 (ms) |
| **2** | **Embedding Time**
(向量嵌入耗時) | RAG 流程延遲 | 進行文字向量化計算與編碼階段的時間。 | 毫秒 (ms) |
| **3** | **Retrieval Time**
(檢索階段耗時) | RAG 流程延遲 | 從知識庫/向量資料庫尋找最匹配知識 Chunk 的搜尋時間。 | 毫秒 (ms) |
| **4** | **Prompt Building Time**
(Prompt 組裝耗時) | RAG 流程延遲 | 將 System Prompt、知識段落 (Context) 與 User Question 組合成 ChatML 格式的時間。 | 毫秒 (ms) |
| **5** | **TTFT**
(Time-to-First-Token, 首字吐出時間) | 關鍵效能指標 | 從使用者按下發送提問，到 LLM 原生引擎吐出**第一個 Token** 所耗費的時間（影響首字卡頓感的關鍵指標）。 | 毫秒 (ms) |
| **6** | **Decode TPS**
(Tokens Per Second, 解碼速度) | 關鍵效能指標 | 扣除首字 (TTFT) 後，模型每秒產出的 Token 數量。
公式：$\frac{\text{tokensCount} - 1}{\text{generationMs} / 1000}$ | tokens/sec |
| **7** | **Total Latency**
(全流程總延遲時間) | 關鍵效能指標 | 從輸入提問到整個答案完全生成結束的總耗時（含預處理、檢索、Prompt 組裝與模型解碼）。 | 毫秒 (ms) |
| **8** | **Generation Latency**
(純模型解碼生成耗時) | 關鍵效能指標 | 從吐出第一個 Token 開始，到最後一個 Token 生成完畢的時間。 | 毫秒 (ms) |
| **9** | **Tokens Count**
(生成 Token 總數量) | 容量與資源 | 本次模型回答所輸出的 Token 總數量。 | Count (個) |
| **10** | **Retrieved Chunks Count**
(檢索切塊數量) | 容量與資源 | 檢索並帶入 Prompt 的參考知識片段/句子數量（Pocket RAG 模式下為 Top-1）。 | Count (塊) |
| **11** | **Allocated RAM**
(記憶體佔用量) | 容量與資源 | 模型推論過程中所分配與消耗的記憶體大小，用以驗證 2GB 記憶體限制裝置的相容性。 | MB |
| **12** | **CPU Threads**
(CPU 執行緒數) | 系統硬體配置 | 當前 GEMM 矩陣計算加速所使用的 CPU 核心數（本專案預設為 4 執行緒）。 | Threads (核) |
| **13** | **Fallback Flag**
(降級/備援狀態) | 系統硬體配置 | 標示推論過程是否因極端狀況（如記憶體不足）而觸發了降級機制。 | Boolean (Bool) |

### 3.2 回答品質（RAGAS）

傳統的 NLP 評測指標（如 BLEU、ROUGE）只能比對生成文字與標準答案的比對相似度，無法判斷「檢索出來的資料對不對」或是「模型有沒有憑空捏造（幻覺）」。

RAGAS 的核心概念是使用 **LLM-as-a-Judge（高階大語言模型擔任裁判）**，將 RAG 拆解為「檢索元件 (Retriever)」與「生成元件 (Generator)」，並從以下核心維度進行獨立量化打分。

#### 3.2.1 評估「生成模組 (Generator)」品質

**Faithfulness (忠實度 / 幻覺率)**：評估 SLM/LLM 生成的回答是否完全基於檢索到的資料。得分低代表模型開始「胡說八道」或出現幻覺。

**Answer Relevance (回答相關性)**：評估模型給出的回答是否精準回應了使用者的提問，有無答非所問或給出大量冗餘廢話。

#### 3.2.2 評估「檢索模組 (Retriever)」品質

**Context Precision (檢索精確度)**：評估向量庫抓出來的資料區塊 (Chunks) 中，有用的資訊是否排在前面，有沒有混入不相關的雜訊。

**Context Recall (檢索召回率)**：評估回答問題所需的關鍵知識，有多大比例被成功從向量庫中檢索出來。

#### 3.2.3 搭配標準答案 (Ground Truth) 的指標

**Factual Correctness (事實正確性)**：將生成的回答與專家寫好的標準答案拆解為獨立事實，比對兩者一致性。

**Semantic Similarity (語意相似度)**：評估生成回答與標準答案的向量語意距離。

## 4. RAGAS 技術與評測手冊

### 4.1 簡介與定位

**定義**：專門用於自動化評估 RAG (檢索增強生成) 系統與語言模型生成品質的開源評測框架。

**核心機制**：採用 LLM-as-a-Judge（大語言模型擔任裁判），將非結構化的生成文字拆解為獨立命題（Claims）與語意向量，進行量化打分（分數介於 `0.0 ~ 1.0`）。

**學術地位**：目前頂級 NLP/AI 期刊與會議（ACL、EMNLP、IEEE、MDPI）評估 RAG 與邊緣端 SLM 的標準主流工具。

### 4.2 核心評估指標 (Core Metrics)

RAGAS 將系統拆解為「檢索端 (Retriever)」與「生成端 (Generator)」進行獨立診斷：

**Faithfulness (忠實度 / 幻覺率)**：評估對象為生成端 (SLM)。其定義與物理意義是用於評估回答是否完全基於檢索到的 Context，有無模型憑空捏造的內容（測幻覺）。必要輸入欄位包含 `response` 與 `contexts`。

**Answer Relevance (回答相關性)**：評估對象為生成端 (SLM)。其定義與物理意義是用於評估生成內容是否切中使用者問題，有無冗餘廢話或答非所問。必要輸入欄位包含 `user_input` 與 `response`。

**Context Precision (檢索精確度)**：評估對象為檢索端 (Vector DB)。其定義與物理意義是用於評估向量庫抓出的切塊 (Chunks) 中，有用資訊是否排在前面。必要輸入欄位包含 `user_input`、`contexts` 與 `reference`。

**Context Recall (檢索召回率)**：評估對象為檢索端 (Vector DB)。其定義與物理意義是用於評估回答所需的關鍵資訊中，有多少比例被成功檢索出來。必要輸入欄位包含 `contexts` 與 `reference`。

**Factual Correctness (事實正確性)**：評估對象為端到端品質。其定義與物理意義是透過自然語言推論 (NLI) 比對生成的回答與專家標準答案 (Ground Truth) 的事實重疊度。必要輸入欄位包含 `response` 與 `reference`。

**Semantic Similarity (語意相似度)**：評估對象為端到端品質。其定義與物理意義是利用 Cross-Encoder 向量模型計算回答與標準答案之間的語意距離。必要輸入欄位包含 `response` 與 `reference`。

## 5. 端到端系統與使用者體驗指標

*(待補齊)*

## 6. 結論與限制

本報告目前完整定義影像辨識模組與 RAG／微調 SLM 模組的效能指標；端到端系統體驗一節的指標定義尚待補齊。

本報告未涵蓋：任何實測數值。實測數據與逐項分析請見〈[系統測試與評估](../README.md#系統測試與評估)〉章節下各報告。
